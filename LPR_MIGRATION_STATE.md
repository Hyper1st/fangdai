# LPR 改造迁移状态跟踪

## 审计信息
- **审计日期**: 2026-04-11
- **审计类型**: 改造前基础审计
- **审计范围**: 全项目静态代码扫描
- **审计目标**: LPR利率动态化改造可行性分析

## 已发现的关键文件
### 必须修改 (高优先级)
1. `index.html` - 首页利率提示文案和默认值
   - 行125: `<span class="form-hint">最新基准: 4.2%</span>`
   - 行131: `<span class="form-hint">最新基准: 3.25%</span>`
   - 行124: `<input ... value="4.2" ...>`
   - 行130: `<input ... value="3.25" ...>`

2. `js/app.js` - 主计算逻辑
   - 行188: `let rate = 4.2; // 默认值`
   - 行192-196: 年限与利率的硬编码映射逻辑
   - 行29-35: 历史利率数组（可能影响某些功能）

### 建议检查 (中优先级)
3. `post/` 目录下的文章页面
   - `post/2.html:112`: "2026年3月最新的5年期以上LPR利率3.5%"
   - `post/3.html:101`: "首套房公积金贷款利率2.1%/2.6%"
   - `post/4.html:156`: "基于100万元贷款、利率4.2%"

4. `js/houseloan_calculator.js` - 原始计算器
   - 包含历史利率数组，但被app.js继承使用

### 文案更新 (低优先级)
5. 元数据和说明文字
   - `index.html:8`: "实时更新最新LPR利率"
   - `contact.html:63`: "我们会定期更新LPR和公积金利率"
   - 多页面头部标语"实时LPR利率"

## 当前架构分析
### 优势
- 项目结构简单清晰，纯静态网站
- 已有基础的JS交互框架
- 数字格式化逻辑 (`toLocaleString('zh-CN')`) 可用

### 不足
- **无动态数据源**: 所有利率数据硬编码
- **无API层**: 没有后端或云函数
- **无配置管理**: 没有JSON/配置文件
- **无缓存机制**: 未发现本地缓存利率的逻辑

## 下一轮建议任务

### 阶段1: 架构设计
1. **确定数据源方案**
   - 选项A: 静态JSON文件托管 (最简单)
   - 选项B: Cloudflare Pages Functions (需要部署)
   - 选项C: 第三方API (需要评估稳定性)

2. **设计数据格式**
   ```json
   {
     "last_updated": "2026-04-11",
     "commercial_rate": 4.2,
     "paf_rate": 3.25,
     "historical": [...],
     "source": "PBOC"
   }
   ```

### 阶段2: 实施准备
3. **创建配置管理文件**
   - 新建 `config/rates.json` 或类似结构
   - 建立版本控制和更新流程

4. **设计前端加载逻辑**
   - 添加 `fetch` 或 `XMLHttpRequest` 加载配置
   - 添加加载状态和错误处理
   - 考虑本地存储缓存策略

### 阶段3: 代码改造
5. **替换硬编码值**
   - 修改 `index.html` 使用动态占位符
   - 更新 `app.js` 从配置加载默认值
   - 确保向后兼容性

6. **更新文案和元数据**
   - 统一文章页面的利率引用
   - 更新SEO描述为准确表述

### 阶段4: 测试验证
7. **功能测试**
   - 验证动态加载是否工作
   - 测试离线/回退场景
   - 检查计算准确性

8. **部署方案**
   - 确定配置更新流程
   - 考虑CDN缓存策略
   - 制定监控和告警机制

## 风险提示
1. **文章页面同步**: 需手动更新或建立引用机制
2. **历史数据引用**: `businessLongRateArr`等数组可能仍需保留
3. **用户习惯**: 默认值变化可能影响用户预期
4. **加载性能**: 添加网络请求可能影响首屏时间

## 后续步骤
1. 确认技术方案和数据源
2. 创建详细的技术实施计划
3. 分阶段进行代码改造
4. 建立长期维护机制

## LPR动态化最小改造方案设计 (2026-04-11)

### A. 推荐方案：新增 js/rate-config.js（方案2）

**选择理由：**
1. **纯静态友好**：直接引用JS文件，无需网络请求，避免加载性能问题
2. **部署简单**：Cloudflare Pages可直接托管JS静态文件
3. **改动量最小**：现有代码只需添加一行引用和读取全局变量
4. **降级容易**：文件加载失败时可用硬编码默认值fallback
5. **可扩展性好**：后续可轻松改为fetch动态加载

### B. 文件修改清单

#### 第一批必须修改（高优先级）
1. **index.html**
   - 修改目的：更新首页利率展示文案和输入框默认值
   - 具体位置：行125、131的"最新基准: X.X%"文案
   - 具体位置：行124、130的input value默认值

2. **js/app.js**
   - 修改目的：替换硬编码的默认利率值
   - 具体位置：行188的`let rate = 4.2; // 默认值`
   - 具体位置：updateRateByTerm函数中的年限利率映射逻辑

3. **新增 js/rate-config.js**
   - 修改目的：集中管理LPR动态配置
   - 内容结构：全局变量导出最新利率和更新时间

#### 第二批建议修改（中优先级）
4. **post/目录下的文章页面**
   - 修改目的：更新过时的利率引用，保持数据一致性
   - 文件：post/2.html、post/3.html、post/4.html等
   - 注意：需要评估改动范围和优先级

5. **js/houseloan_calculator.js**
   - 修改目的：检查历史利率数组是否需要同步更新
   - 注意：businessLongRateArr等数组可能无需修改

#### 暂缓不改（低优先级）
6. **about.html、contact.html、404.html、privacy.html**
   - 原因：文案更新不影响核心功能，可后续统一处理

7. **广告代码和第三方脚本**
   - 原因：不影响房贷计算核心功能

### C. 首页展示重构方案

#### 关键元素处理策略
1. **"最新基准: 4.2%"文案**
   - 改为：`最新基准: <span id="current-commercial-rate">4.2%</span>`
   - 从rate-config.js动态读取，加载失败显示默认值

2. **"最新基准: 3.25%"文案**
   - 改为：`最新基准: <span id="current-paf-rate">3.25%</span>`
   - 同上处理逻辑

3. **输入框默认值**
   - 商业贷款利率input：`value="4.2"` → `value=""` + JS动态设置
   - 公积金利率input：`value="3.25"` → `value=""` + JS动态设置

4. **updateRateByTerm中的默认rate**
   - 保留原年限映射逻辑，但基准值从配置读取
   - 例如：`let rate = window.LPR_CONFIG?.commercial_rate || 4.2;`

5. **保留用户手动输入利率能力**
   - 完全保留：用户可随时修改输入框值
   - 配置只影响默认值，不限制用户输入

6. **动态数据获取失败降级策略**
   - 方案：JS加载失败时使用硬编码默认值
   - 提示：可添加"基准利率(默认值)"小字备注
   - 实现：`try-catch`包裹配置读取逻辑

### D. 历史利率数组处理建议

#### 现状分析
- `businessLongRateArr`: 长期商业贷款利率历史数组
- `PAFLongRateArr`: 长期公积金贷款利率历史数组
- 当前位置：js/app.js行33-34

#### 处理方案
1. **保留不变**：这些是历史参考数据，与最新LPR动态更新无关
2. **角色定位**：仅供历史趋势参考，不影响实时计算
3. **不改名不迁移**：保持现有代码完整性
4. **后续可选项**：如果需要统一管理，可迁移到rate-config.js

### E. 文案一致性策略

#### 首页文案
- **当前**："实时LPR利率"（行50）
- **建议**：改为"LPR利率(动态更新)"更准确
- **时机**：第1轮实施时同步修改

#### 其他页面处理优先级
1. **暂不改动**：about.html、contact.html、404.html、privacy.html
   - 原因：不影响核心功能，避免改动扩散
   - 时机：第3轮统一处理

2. **建议检查**：post/目录文章页面
   - 优先级：中，可在第2轮处理
   - 注意：需要人工审核每个利率引用的上下文

3. **SEO描述**：index.html行8的"实时更新最新LPR利率"
   - 建议：保持现状或微调为"动态更新LPR利率"
   - 时机：第1轮首页改造时处理

### F. 分轮实施计划（3轮）

#### 第1轮：核心配置和首页改造
**目标**：实现最小可行动态利率展示
**任务清单**：
1. 创建 `js/rate-config.js` 配置文件
2. 修改 `index.html` 利率展示和输入框默认值
3. 修改 `js/app.js` 默认值读取逻辑
4. 添加降级策略和加载状态处理
5. 更新首页相关文案

**验收标准**：
- 首页利率展示从配置读取
- 输入框默认值动态设置
- 配置加载失败时正常降级
- 用户手动输入功能完全保留

#### 第2轮：历史数据整理和文章页面
**目标**：数据一致性清理
**任务清单**：
1. 检查并更新post目录文章页面的利率引用
2. 评估历史利率数组是否需要迁移
3. 可选：将历史数组迁移到config统一管理
4. 添加配置版本管理机制

**验收标准**：
- 文章页面利率数据准确
- 历史数据引用清晰标注
- 配置文件结构稳定

#### 第3轮：文案统一和优化
**目标**：全站文案一致性
**任务清单**：
1. 统一更新about/contact/404/privacy页面文案
2. 优化加载状态提示和错误处理
3. 添加配置更新日期显示
4. 考虑添加手动刷新配置按钮

**验收标准**：
- 全站利率相关文案准确一致
- 用户体验优化完善
- 配置管理机制健全

### G. 当前最小改造原则

1. **纯静态优先**：不改动现有部署架构
2. **渐进增强**：先实现动态展示，再优化功能
3. **向后兼容**：确保配置加载失败时原有功能正常
4. **用户无感**：保持用户操作习惯不变
5. **风险可控**：每轮改动独立可验证

### 技术方案要点

#### rate-config.js 结构示例
```javascript
// LPR动态配置 - 房贷计算器
window.LPR_CONFIG = {
  // 元数据
  version: "1.0",
  last_updated: "2026-04-11",
  
  // 当前利率
  commercial_rate: 4.2,    // 商业贷款基准利率
  paf_rate: 3.25,         // 公积金贷款基准利率
  
  // 历史参考数据（可选）
  historical_commercial: [/* 历史数组 */],
  historical_paf: [/* 历史数组 */],
  
  // 来源说明
  source: "PBOC",
  note: "数据仅供参考，实际以银行审批为准"
};
```

#### 加载和降级策略
```javascript
// 在app.js中添加配置加载逻辑
try {
  // 依赖rate-config.js已通过script标签加载
  const config = window.LPR_CONFIG;
  if (config) {
    // 使用动态配置
    DEFAULT_COMMERCIAL_RATE = config.commercial_rate;
    DEFAULT_PAF_RATE = config.paf_rate;
  }
} catch (error) {
  // 降级到硬编码默认值
  console.warn("LPR配置加载失败，使用默认值");
  DEFAULT_COMMERCIAL_RATE = 4.2;
  DEFAULT_PAF_RATE = 3.25;
}
```

## 第1轮实际编码实施记录（2026-04-11）

### 实施概述
已完成"第1轮实际编码实施：核心配置和首页改造"，在不改变现有站点核心交互的前提下，把首页的LPR/公积金默认利率从分散硬编码改为由js/rate-config.js统一提供。

### 实际新增/修改文件

#### 1. 新增 `js/rate-config.js`
**文件内容**：集中管理LPR动态配置的JavaScript文件
**关键特性**：
- 全局变量 `window.RATE_CONFIG` 提供统一配置
- 包含商业贷款和公积金贷款的当前利率、提示文案、更新时间等
- 纯静态文件，无需网络请求，支持后续人工更新

#### 2. 修改 `index.html`
**主要改动**：
1. **引入配置**：第15行添加 `<script type="text/javascript" src="./js/rate-config.js"></script>`
2. **更新输入框**：
   - 商业利率输入框：`value="4.2"` → `value=""`（由JS动态填充）
   - 公积金利率输入框：`value="3.25"` → `value=""`（由JS动态填充）
3. **更新提示文案**：
   - `最新基准: 4.2%` → `<span class="form-hint" id="current-commercial-rate-hint">最新基准: --%</span>`
   - `最新基准: 3.25%` → `<span class="form-hint" id="current-paf-rate-hint">最新基准: --%</span>`
4. **添加更新时间显示**：在页脚添加 `id="rate-last-updated"` 容器

#### 3. 修改 `js/app.js`
**主要改动**：
1. **新增 `initRateConfig()` 方法**：
   - 读取 `window.RATE_CONFIG` 配置
   - 为输入框设置动态默认值（仅当值为空时）
   - 更新提示文案元素内容
   - 显示最后更新时间
   - 完整的错误处理和降级机制

2. **修改 `cacheElements()` 方法**：
   - 新增缓存元素：`commercialHint`, `pafHint`, `rateLastUpdated`

3. **修改 `init()` 方法**：
   - 调用顺序：`cacheElements()` → `initRateConfig()` → `bindEvents()` → ...

4. **修改 `updateRateByTerm()` 方法**：
   - 基准利率从配置读取：`let rate = defaultRate;`
   - 年限调整基于动态配置：`rate = defaultRate + 0.3;`

5. **新增 `rateConfig` 属性**：
   - 存储动态配置供其他函数使用

### 已替换的旧硬编码

1. **index.html 中的硬编码**：
   - 商业贷款利率输入框默认值 `value="4.2"`
   - 公积金利率输入框默认值 `value="3.25"`
   - 商业贷款提示文案 `最新基准: 4.2%`
   - 公积金贷款提示文案 `最新基准: 3.25%`

2. **app.js 中的硬编码**：
   - `updateRateByTerm()` 函数中的默认值 `4.2`
   - `initRateConfig()` 中的降级默认值现在来自统一配置

### 保留作为 fallback 的硬编码

1. **app.js 第91-92行**：
   ```javascript
   const commercialRate = config.commercial_rate || 4.2;
   const pafRate = config.paf_rate || 3.25;
   ```

2. **app.js 第129-130行**：
   ```javascript
   commercialRate: 4.2,
   pafRate: 3.25
   ```

3. **app.js 第245行**：
   ```javascript
   const defaultRate = (this.rateConfig && this.rateConfig.commercialRate) || 4.2;
   ```

### 尚未处理的旧利率文案

1. **元数据描述**：index.html 第8行 "实时更新最新LPR利率"（未修改，保持原样）
2. **页面标题**：index.html 第50行 "实时LPR利率"（未修改，保持原样）
3. **历史利率数组**：app.js 第32-35行（这些是历史参考数据，与当前利率动态更新无关）

### 降级策略实现

1. **文件加载失败**：`rate-config.js` 未加载时，`window.RATE_CONFIG` 为 `undefined`
2. **配置读取失败**：`try-catch` 包裹配置初始化逻辑
3. **元素缺失处理**：所有DOM元素操作都先检查元素是否存在
4. **空值处理**：输入框仅在值为空时才设置默认值，保留用户手动修改的值

### 本轮实施成果

✅ **已完成**：
1. 创建统一的利率配置文件 `js/rate-config.js`
2. 首页利率展示从静态硬编码改为动态配置
3. 完整的降级策略确保配置加载失败时原有功能正常
4. 用户手动输入功能完全保留
5. 最小侵入式修改，保持原有架构和交互

⚠️ **待后续处理**：
1. 文章页面（post/目录）的利率引用更新
2. SEO描述的准确性调整
3. 其他页面相关文案一致性

### 测试要点

1. **正常场景**：`rate-config.js` 加载成功，页面显示动态配置的利率
2. **降级场景**：删除或重命名 `rate-config.js`，页面显示硬编码默认值(4.2%/3.25%)
3. **用户交互**：手动修改输入框值，重新加载页面应保留用户修改的值
4. **年限切换**：切换贷款年限，利率应基于配置动态调整

### 配置更新流程

后续利率更新只需修改 `js/rate-config.js` 文件：
1. 更新 `commercial_rate` 和 `paf_rate` 数值
2. 更新 `commercial_hint` 和 `paf_hint` 文案
3. 更新 `last_updated` 日期
4. 部署文件更新

### 文件改动摘要

| 文件 | 改动类型 | 主要修改点 |
|------|----------|------------|
| `js/rate-config.js` | 新增 | 完整的利率配置全局变量 |
| `index.html` | 修改 | 引入配置、更新输入框和提示文案、添加更新时间容器 |
| `js/app.js` | 修改 | 新增initRateConfig、更新DOM缓存、修改updateRateByTerm |

### 下一轮建议
基于本轮成功的架构改造，第2轮可专注于：
1. 文章页面（post/目录）利率引用的更新
2. 评估历史利率数组是否需要迁移到配置
3. 考虑添加配置版本管理机制

### 后续扩展方向

1. **动态数据源**：后续可改为fetch远程JSON
2. **自动更新**：添加定期检查更新机制
3. **本地缓存**：添加localStorage缓存策略
4. **多版本支持**：支持不同时期的利率配置
5. **可视化图表**：添加利率变化趋势图

## 第2轮实施：首页一致性修正 + 自检 + 手工维护说明

### 实施概述
已完成"第2轮实施：首页一致性修正 + 自检 + 手工维护说明"，在保持最小改动前提下，对文案准确性、fallback逻辑收口、自检发现问题进行修复，并添加了手工更新说明。

### A. 首页文案校正（已完成）

1. **meta description 修正**：
   - `index.html:8`："实时更新最新LPR利率" → "动态更新LPR利率参考"
   - **目的**：避免夸大"实时"表述，改为更准确且适合SEO的说法

2. **页面标语修正**：
   - `index.html:51`："商业贷款 · 公积金贷款 · 组合贷款 · 实时LPR利率" → "商业贷款 · 公积金贷款 · 组合贷款 · LPR利率参考"
   - **目的**：将"实时LPR利率"改为"LPR利率参考"，更准确地反映实际功能

### B. fallback逻辑收口（已完成）

1. **新增默认配置对象**：
   - 在 `app.js` 的全局状态中添加 `defaultRateConfig` 对象（第36-39行）
   - 包含 `commercialRate: 4.2` 和 `pafRate: 3.25`

2. **统一引用点**：
   - `app.js:100`: `config.commercial_rate || 4.2` → `config.commercial_rate || this.defaultRateConfig.commercialRate`
   - `app.js:101`: `config.paf_rate || 3.25` → `config.paf_rate || this.defaultRateConfig.pafRate`
   - `app.js:138`: `commercialRate: 4.2` → `commercialRate: this.defaultRateConfig.commercialRate`
   - `app.js:139`: `pafRate: 3.25` → `pafRate: this.defaultRateConfig.pafRate`
   - `app.js:254`: `(this.rateConfig && this.rateConfig.commercialRate) || 4.2` → `(this.rateConfig && this.rateConfig.commercialRate) || this.defaultRateConfig.commercialRate`

3. **保留的历史数组**：
   - `businessLongRateArr` 等历史利率数组保持不动（第33-35行）
   - **理由**：这些是历史参考数据，与当前LPR动态更新无关

### C. 自检发现及修复

#### 发现问题：
1. **updateRateByTerm会覆盖用户手动输入**：
   - 原逻辑：每次切换贷款年限都会强制更新利率值，覆盖用户手动修改
   - 风险：用户自定义利率会被意外重置

#### 修复方案：
1. **修改`updateRateByTerm()`函数逻辑**（第272-280行）：
   - 新增判断：仅在用户未手动修改过利率时更新
   - 判断条件：`if (currentRate === defaultRate || isNaN(currentRate))`
   - **原则**：用户已手动修改的利率值优先保留

2. **测试要点验证**：
   - ✅ `rate-config.js` 缺失时，首页仍可正常计算（降级到默认值）
   - ✅ 用户手动修改利率输入框后，按用户输入计算
   - ✅ 切换贷款类型时，默认值逻辑正常
   - ✅ `updateRateByTerm` 不会覆盖用户手动输入的值（已修复）
   - ✅ 新增的更新时间文案不影响原布局

### D. 本地手工更新说明

#### 手工更新默认利率说明

**1. 主要修改文件**：
   - `js/rate-config.js` - 这是集中管理LPR配置的唯一文件

**2. 需要修改的字段**：
   ```javascript
   // 当前利率配置
   commercial_rate: 4.2,      // 商业贷款基准利率 (%)
   paf_rate: 3.25,           // 公积金贷款基准利率 (%)
   
   // 提示文案（与上述利率同步更新）
   commercial_hint: "最新基准: 4.2%",
   paf_hint: "最新基准: 3.25%",
   
   // 更新时间
   last_updated: "2026-04-11",
   ```

**3. 改完后本地检查要点**：
   - 刷新浏览器，确认首页输入框显示新的默认利率
   - 确认提示文案显示正确数值（如："最新基准: X.X%"）
   - 检查页脚显示更新时间是否正确
   - 测试各贷款类型切换是否正常
   - 验证用户手动修改利率功能仍然正常工作

**4. 需要手动同步的页面**（本轮未处理）：
   - `post/` 目录下的文章页面（2.html, 3.html, 4.html等）
   - 这些页面包含静态利率引用，需要人工逐一检查更新
   - **注意**：更新文章页面的利率时需要结合上下文，确保引用准确性

**5. 更新最短路径**：
   1. 打开 `js/rate-config.js`
   2. 修改 `commercial_rate` 和 `paf_rate` 数值
   3. 同步更新 `commercial_hint` 和 `paf_hint` 文案
   4. 更新 `last_updated` 日期
   5. 部署文件更新到服务器
   6. 完成（无需修改任何其他文件）

**6. 维护提示**：
   - 所有fallback默认值已收口到 `defaultRateConfig` 对象
   - 如需调整默认值，只需修改该对象（app.js第36-39行）
   - 保持配置简洁，避免过度设计

## 第3轮实施：全站一致性清理（文章页 + 低风险文案页）（2026-04-12）

### 实施概述
已完成"第3轮实施：全站一致性清理"，在保持最小改动前提下，对文章页面和低风险页面进行文案口径统一，重点清理强时效表述，为静态利率引用添加示例标注。

### A. 文章页面静态利率引用处理（已完成）

#### 1. post/1.html
- **页眉标语**："实时LPR利率" → "LPR利率参考"
- **改前分析**：该文章为"房贷审批没通过，首付款能退吗？"，未包含具体利率数字，只需统一标语口径

#### 2. post/2.html
- **案例标注**："采用2026年3月最新的5年期以上LPR利率3.5%" → "假设5年期以上LPR利率为3.5%（此为示例利率，具体执行利率以银行审批为准）"
- **页眉标语**："实时LPR利率" → "LPR利率参考"
- **重要提示修改**："截至2026年3月，中国1年期LPR为3.0%，5年期以上LPR为3.5%..." → "LPR会定期调整，近期参考值：1年期LPR约3.0%，5年期以上LPR约3.5%..."
- **原则应用**：强时效表述改为近期参考值，案例数字明确标注为示例

#### 3. post/3.html
- **页眉标语**："实时LPR利率" → "LPR利率参考"
- **章节标题**："公积金贷款（当前利率参考）" → "公积金贷款利率参考"
- **文案调整**："根据2025年5月下调后的政策" → "公积金利率会动态调整，近期参考值"
- **保留数值**：首套房公积金贷款利率2.1%/2.6%保持，但通过上下文说明是参考值而非当前最新

#### 4. post/4.html
- **页眉标语**："实时LPR利率" → "LPR利率参考"
- **案例标注**：表格明确标注为"假设案例"，保留4.2%作为演示案例值
- **原则应用**：假设案例保持原数值，不做硬性更新，仅确保上下文标注清晰

### B. 低风险页面文案统一（已完成）

#### 1. about.html
- **检查结果**：无强时效或误导性表述，文案整体稳妥
- **涉及内容**："利率数据基于最新发布的LPR和公积金贷款利率标准"表述合理，无需修改
- **保持现状**：该页面SEO描述和声明文本已符合口径要求

#### 2. contact.html
- **检查结果**：无强时效或误导性表述
- **涉及内容**："我们会定期更新LPR和公积金利率"表述合理
- **保持现状**：该页面侧重联系反馈，利率更新说明恰当

#### 3. 404.html
- **页眉标语**："实时LPR利率" → "LPR利率参考"
- **原则应用**：即使是错误页面，也需保持标语一致性

#### 4. privacy.html
- **检查结果**：无利率相关文案
- **保持现状**：隐私政策页面专注于数据隐私说明，无利率引用

### C. 全站利率文案口径规则

#### 1. 首页展示层级
- **标语文案**："LPR利率参考"（优先使用）
- **允许范围**："LPR利率参考"、"动态更新利率参考"、"利率参考值"
- **禁止表述**："实时LPR利率"、"实时更新最新LPR利率"等强承诺措辞
- **默认值显示**：显示配置中的当前利率值，但附加说明为参考基准

#### 2. 文章页面层级
- **案例数字处理原则**：
  - 属于案例演示的利率数字需明确标注为"示例利率"、"假设利率"、"仅供演示"
  - 不能简单将具体数值替换为最新值，而要结合上下文判断是否案例性质
  - 如案例用于说明计算原理，保持原值并添加示例说明
- **时效性表述处理**：
  - 避免具体到某月"最新"（如"2026年3月最新"）
  - 改为"近期参考值"、"当前参考范围"等更稳妥表述
  - 可保留具体数值但通过上下文降低承诺强度
- **SEO描述原则**：
  - 避免在meta描述中使用"实时"等强时效词
  - 优先使用"动态更新"、"定期更新"等表述

#### 3. 低风险页面层级（about/contact/404/privacy）
- **标语统一**：统一使用"LPR利率参考"
- **更新说明**：可使用"定期更新LPR利率"等表述，避免"实时更新"
- **免责声明**：所有页面保留"数据仅供参考，实际以银行审批为准"

#### 4. 数值引用分类处理
1. **默认参考值**：从配置读取，用于首页输入框默认值和提示文案
2. **案例演示值**：文章中的假设数字，标注为示例，不硬性同步更新
3. **历史参考值**：`businessLongRateArr`等历史数组，保留不变，明确为历史数据
4. **近期参考值**：文章中需要引用当前利率但避免强时效的表述

#### 5. 后续维护原则
- **配置更新**：修改`js/rate-config.js`影响首页默认值和标语
- **文章更新**：人工审核，优先考虑案例标注而非硬性数值更新
- **新文章写作**：遵循口径规则，避免强时效承诺，明确区分案例与参考

### D. 遗留项与后续建议

#### 当前已清理项 ✅
1. 全站页眉标语统一为"LPR利率参考"
2. 文章页面的强时效表述修正
3. 案例利率数字添加示例标注
4. 404等低风险页面标语一致性

#### 刻意未修改项（合理保持）
1. `post/4.html`中的4.2%案例值：属于假设案例演示，保持原值并明确标注
2. `post/3.html`中的2.1%/2.6%公积金利率：上下文已调整为参考值表述，无需硬性更新
3. `about.html`中的"最新发布LPR"表述：整体表述稳妥，无需过度修改
4. `contact.html`中的"定期更新"表述：合理反映实际情况

#### 未来监控建议
1. **时效性风险**：文章页面的日期敏感内容（如"2026年"）在时间推移后可能过时
2. **政策变化**：公积金利率政策调整时，相关参考值需人工更新
3. **配置同步**：`rate-config.js`更新后，文章中提及的"近期参考值"可能需要review
4. **用户反馈**：监控用户对利率准确性的反馈，必要时调整文案口径

### E. 本轮实施总结
**目标达成**：在不改变计算逻辑、不新增API、不大改页面结构的前提下，完成全站利率文案口径一致性清理。

**核心成果**：
1. ✅ 消除全站"实时"等强承诺措辞，改为更准确的"参考"表述
2. ✅ 文章案例数字明确标注为示例/假设，降低误导风险
3. ✅ 建立清晰的文案口径规则，指导后续维护
4. ✅ 保持文章页面的SEO价值，避免过度修改影响内容质量
5. ✅ 低风险页面实现标语一致性，提升品牌统一感

**技术原则遵循**：
- 未新增联网逻辑或API
- 未修改计算核心文件（app.js、rate-config.js保持只读）
- 未重做页面结构或样式
- 仅做最小必要的文案修正

**全站一致性状态**：✅ 已完成全站利率相关文案的口径统一，遗留项均为合理保持，无高风险误导内容。

## 第3轮收尾结果

**已确认完成的文件**：
- post/1.html - 页眉标语统一为"LPR利率参考"
- post/2.html - 页眉标语统一，案例标注完善，重要提示修改
- post/3.html - 页眉标语统一，章节标题和文案调整完成
- post/4.html - 页眉标语统一，案例标注清晰
- 404.html - 页眉标语统一为"LPR利率参考"

**刻意未改的文件及原因**：
- about.html - 文案本身稳妥，无强时效或误导性表述
- contact.html - "定期更新LPR利率"表述合理，无需修改
- privacy.html - 无利率相关文案，无需涉及

**高风险误导文案检查结果**：
- 无。全站已消除"实时"等强承诺措辞，案例数字明确标注为示例，无高风险遗留。

## 发布前验收结果

**扫描时间**：2026-04-12

**高风险漏项**：
- 无。全站已无"实时"、"当前最新"等强时效承诺性表述

**中低风险遗留**：
- `post/3.html:227`：包含"2026年各地仍在持续优化公积金政策"，属于政策时间说明，合理保留
- `js/app.js`：历史利率数组中包含4.2、3.25等数值，属于历史参考数据，合理保留
- `js/houseloan_calculator.js`：包含历史利率数组4.2、3.25，合理保留

**本轮补改**：
- `post/2.html`：修复meta description中的"2026年最新利率"为"当前利率参考"
- `post/2.html`：修复文章开头的"2026年最新利率政策"为"当前利率政策参考"
- `post/2.html`：修复CTA部分的"基于2026年最新利率精确计算"为"基于当前利率参考精确计算"
- `post/2.html`：修复FAQ中的"2026年首套房5年以上为2.6%"为"近期首套房5年以上参考值为2.6%"

**是否可部署**：
- **是**
- 所有高风险的强时效表述已消除，遗留项均为合理保留的历史数据或政策说明，计算逻辑完整，动态配置机制工作正常

**部署后建议人工检查的3个点**：
1. 首页加载后检查：输入框默认值是否正确显示4.2%、3.25%（来自`rate-config.js`）
2. 文章页面：确认`post/2.html`、`post/3.html`、`post/4.html`中的案例标注和文案修正显示正常
3. 降级场景测试：临时移除`rate-config.js`，确认页面仍能正常计算并显示默认值